let isOn = true;

if (isOn == true) {
    console.log('the light is on!');
};


isOn = false;


if (isOn){
    console.log("the light is on!");
};
if (isOn == false){
    console.log("the light is off")
};

weather =65;
if (weather <70) {
    console.log('wear a jacket');

};

if (weather < 70) {
    console.log("wear a jacket");

}else {// else can follow and if statement and will fire
//if the above statements didn't
console.log("No jacket necessary!");
}

/*
Bronze:
Write an if else statement that checks your name;
If it is your name, console log '<name>'
If the name does not match, console.log 'Hello, what is your name?'
Silver:
If It is your name, console log 'Hello, my name is <name>'
Gold:
If it is not your name, console log 'Hello, is your name <name here> ?'
*/

var name="Alex";
if (name!== "Pam"){
    console.log('Hello, what is your name?');

} else{
    console.log(name);
}
/*
Use this string: let name = 'zAchARy';
Bronze:
Write an if else statement that looks at the first letter of a string. If it is uppercase, console.log the string;
If it is not console.log 'hey, this isn't written correctly'
Silver:
If the first letter of a string is uppercase, only console.log that letter
if it is not, console.log the rest of the string Without the first letter, and change them to lowercase
Gold:
If the first letter of a string is uppercase, console.log that that first letter of a string  plus the rest of the string to lowercase
If it is not, console.log the first letter of the string to uppercase plus the rest of the string to lowercase
*/

let student ={
    name: "justin",
    grade: 12
};
console.log(student.name)

//on quiz  (object or arrays)
let names = [
    "Justin",
    "Adam",
    "Amy"
]
//ways to access an object
console.log(student.name)
console.log(student[name]);


//ways to access an array
console.log9names[2]





let fruit = ['apple','orange']
let apple = fruit[0]
console.log(apple);



//Bronze
let name = 'zAchARy';
console.log(name.charAt(0));
console.log(name[0]);

if (name[0]== name[0].toUpperCase()) {
    console.log(name);
} else {
    console.log("hey, this isn't written correctly");

};

//Silver
let name = 'zAchARy';

if (name[0] == name [0]).toUpperCase()) {
 console.log(name[0])
 else}   {
     console.log(name.slice91).toLowerCase());
 }

//Gold 
let name = 'zAchARy';

if (name[0]==name[0].toUpperCase()){
    let isUppercase = name[0] + name.slice(1).toLowerCase();
console.log('console log #1;', isUpperCase()+name.
slice(1).toLowerCase();
console.log('console log #2:', notUppercase);
};

//switch
let officeCharacter  ="Pam"

switch(officeCharacter) {
    //kinda like if officeChacter =="Michael"
}
case "Michael"
console.log("My mind is going a mile an hour");
break;
case "dwight":
    console.log(Perfectenschlang");
    break; //break will leave the switch after the clock runs
    case "jim";
    console.log(Bears, Beets. Battlestar Galactica");
    console.log('I'm sorry, ${officeCharacter}, but do i know you?);

    let dessert - 'carrots';

    switch (dessert) }
    case'pie':
    console.log('Pie,pie,me of my!');
    case'cake':
    console.log('cake is great!');
    break;
    case 'ice cream':
        console.log('I scream for ice cream!')
        defult:
        console.log('Not on the menu.');
}

//else if tangent
if (dessert=="cake){}




























